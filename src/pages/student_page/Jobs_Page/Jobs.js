import { Box } from "@mui/material";

export default function Jobs(){
    return(
        <Box component={'span'}style={{position:'relative',marginLeft:'42rem',width:'max-content',marginTop:'-1000px',fontFamily:'poppins',fontSize:'5rem'}}>Jobs Page</Box>
    );
};