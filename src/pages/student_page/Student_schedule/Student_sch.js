import { Box } from "@mui/material";

export default function Student_sch(){
    return(
        <Box component={'span'}style={{position:'relative',marginLeft:'42rem',width:'max-content',marginTop:'-80vh',fontFamily:'poppins',fontSize:'5rem'}}>Student schedule page</Box>
    );
}